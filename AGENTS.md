Use `src/components/Logo.tsx` for inline, theme-colored brand marks; keep the matching SVG assets under `public/` for direct use, because external SVG images cannot inherit the app's text color.
Keep share-image and page URL metadata in leaf route heads, not the root head, so each page owns its preview; the home share image is the rasterized `public/og.png` based on LogoFull.- Leaflet map (src/components/EventsMap.tsx) is lazy-loaded behind ClientOnly and uses react-leaflet v5; v4 crashes under React 19 ("Map container is already initialized").
- UI copy lives in `src/i18n/en.ts` (flat dotted keys) and is read via `useLang().t()` from `src/i18n/index.tsx`; ru/lv are partial and fall back to EN — keeps all languages in one no-dependency system.
- Map tiles use Esri World Dark/Light Gray Canvas with matching attribution because CARTO now watermarks its keyless tiles instead of serving a usable basemap.
